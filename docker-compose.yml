
services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile-frontend
    ports:
    - 3000:3000
    environment:
    - REACT_APP_GOOGLE_MAPS_API_KEY=AIzaSyBtsc3WDY5kekAriLEtXcXe5YER52HO8RY
    - REACT_APP_API_BASE_URL=http://localhost:8000/api    
    # volumes:
    # - ./src:/app
    depends_on:
    - backend

  backend:
    build:
      context: .
      dockerfile: Dockerfile-backend
    ports:
    - 8000:8000
    # volumes:
    # - .:/app
 
    ## look at how to specify environmental variables 